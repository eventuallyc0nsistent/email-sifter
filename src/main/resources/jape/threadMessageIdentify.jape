Phase: GetThreads
Input: Token Thread
Options: control = appelt

Rule: threadMessageIdentify
(
	(
		{Thread}
		(
			({Token, !Thread.category == "Thread Header"})+
		):threadbody
		
	):thread
	{Thread}
)

-->

:threadbody.ThreadPart = {rule="Thread", category = "Thread Body"},
:thread.Thread = {rule="Thread", category = "Thread Main"}